# Normalise to LF in the repository. The generators write LF, and the CI job
# regenerates data/ and diffs it, so a CRLF checkout would fail spuriously.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
